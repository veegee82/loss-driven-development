# Task

A colleague sent you a C+ draft design doc at `docs/architecture.md`. A design review is in 30 minutes. Make it review-ready (B+ or better), but do NOT rewrite from scratch — preserve what works.

Apply LDD discipline — this is refinement (y-axis). You should invoke `iterative-refinement` with a real structured gradient (named defects + locations), explicit budget, stop conditions.

Emit the full LDD trace block with the refinement-flavored header. Append trace lines to `.ldd/trace.log`. When closed, write `run-summary.md`.
